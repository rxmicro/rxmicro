package io.rxmicro.examples.data.r2dbc.postgresql.model.fields.reflection.model;

import io.r2dbc.spi.Row;
import io.r2dbc.spi.RowMetadata;
import io.rxmicro.data.sql.r2dbc.detail.EntityFromR2DBCSQLDBConverter;
import io.rxmicro.examples.data.r2dbc.postgresql.model.fields.Status;

import java.math.BigDecimal;
import java.math.BigInteger;
import java.net.InetAddress;
import java.time.Instant;
import java.time.LocalDate;
import java.time.LocalDateTime;
import java.time.LocalTime;
import java.time.OffsetDateTime;
import java.time.ZonedDateTime;
import java.util.UUID;

import static rxmicro.$$Reflections.setFieldValue;

/**
 * Generated by {@code RxMicro Annotation Processor}
 */
public final class $$EntityEntityFromR2DBCSQLDBConverter extends EntityFromR2DBCSQLDBConverter<Row, RowMetadata, Entity> {

    public Entity fromDB(final Row dbRow,
                         final RowMetadata metadata) {
        final Entity model = new Entity();
        setFieldValue(model, "id", dbRow.get(0, Long.class));
        setFieldValue(model, "status", toEnum(Status.class, dbRow.get(1, String.class), "status"));
        setFieldValue(model, "aBoolean", dbRow.get(2, Boolean.class));
        setFieldValue(model, "aByte", dbRow.get(3, Byte.class));
        setFieldValue(model, "aShort", dbRow.get(4, Short.class));
        setFieldValue(model, "aInteger", dbRow.get(5, Integer.class));
        setFieldValue(model, "aLong", dbRow.get(6, Long.class));
        setFieldValue(model, "bigInteger", dbRow.get(7, BigInteger.class));
        setFieldValue(model, "aFloat", dbRow.get(8, Float.class));
        setFieldValue(model, "aDouble", dbRow.get(9, Double.class));
        setFieldValue(model, "bigDecimal", dbRow.get(10, BigDecimal.class));
        setFieldValue(model, "character", dbRow.get(11, Character.class));
        setFieldValue(model, "string", dbRow.get(12, String.class));
        setFieldValue(model, "instant", dbRow.get(13, Instant.class));
        setFieldValue(model, "localTime", dbRow.get(14, LocalTime.class));
        setFieldValue(model, "localDate", dbRow.get(15, LocalDate.class));
        setFieldValue(model, "localDateTime", dbRow.get(16, LocalDateTime.class));
        setFieldValue(model, "offsetDateTime", dbRow.get(17, OffsetDateTime.class));
        setFieldValue(model, "zonedDateTime", dbRow.get(18, ZonedDateTime.class));
        setFieldValue(model, "inetAddress", dbRow.get(19, InetAddress.class));
        setFieldValue(model, "uuid", dbRow.get(20, UUID.class));
        return model;
    }
}
